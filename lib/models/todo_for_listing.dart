class TodoForListing {
  String todoID;
  String todoTitle;
  DateTime createDateTime;
  DateTime lastEditedDateTime;

  TodoForListing({this.todoID, this.todoTitle, this.createDateTime, this.lastEditedDateTime});

}